-
  var{ boxContent, boxTitle, sectionTitle, socialList } = data.content.connect
include ../header/header
include ../layout/base

block awesome-content
  .text
    .text__info-box
      button.button-transparent
        .icoana
          a(href="https://www.youtube.com/watch?v=GAdak4L8gsA" title="bankai" target="_blank")
            .cv-youtube

    .flex-gap16
      .box__gap-8
        .box__info-box
          .text__section-title= boxContent.boxTitle
        .box__gap-8
          each content in boxContent.content
            .box__descriere= content
      .flex
        .flex-row
          each social, key in socialList
            input.connect-radio(
              class=`cv-${social.icon}`
              type="radio"
              id=`connect-radio-${key}`
              name="radio-c"
            )
          each social, key in socialList
            .box-body(data-box=`iii-${key+1}`)
              .flex
                .flex-row-space
                  div= social.title
                  input.connect-radio-reset(
                    name="radio-c"
                    id="reset"
                    type="radio"
                  )
                div= social.description
                .line
                .flex-row
                  div(class=`cv-${social.icon}`)
                  a(href=`${social.link}`)= social.label

      button.button-rosu
        .icoana
          a(href="https://www.instagram.com/alexcercel22/" title="contact me" target="_blank")
            .cv-instagram
        | Let's chat!
